export const VIDEO_LIMITS = {
  allowedExtensions: ['.mp4', '.mov', '.avi'],
};

export const validateVideo = file => {
  // a placeholder implementation
  return true;
};

export const processVideoForUpload = file => {
  // a placeholder implementation
  return file;
};

const video = {};
export default video;
